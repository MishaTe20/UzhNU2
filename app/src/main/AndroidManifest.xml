<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mishate.uznuintime">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.mishate.InTime.MainActivity" />
        <activity android:name="com.example.mishate.InTime.call_schedule">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.mishate.InTime.profile_page" />
        <activity android:name="com.example.mishate.InTime.Signup_form" />
        <activity android:name="com.example.mishate.InTime.login_form" />
        <activity android:name="com.example.mishate.InTime.reset_password"></activity>
    </application>

</manifest>